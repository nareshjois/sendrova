<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow http:// for local wrangler (LAN IP / emulator 10.0.2.2). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
