# Copy to `.dev.vars` for local `wrangler dev` (`.dev.vars` is gitignored).
# Production must use `wrangler secret put TOKEN_SIGNING_KEY` — never commit real secrets.
TOKEN_SIGNING_KEY=dev-token-signing-key-change-me
