# Build Windows Electrobun desktop + Android signed release APK and attach to a GitHub Release.
#
# Triggers:
#   - Push tags matching v* (e.g. v1.0.0, v1.0.0-canary.1)
#   - workflow_dispatch (manual; optional tag + channel)
#
# Channel:
#   - Tag contains "canary" (or dispatch channel=canary) → bun run build:canary
#   - Otherwise → bun run build:stable (needed for /releases/latest auto-update)
#
# Versioning (stable baseline 1.0.0):
#   - Stable:  v1.0.0, v1.0.1, …
#   - Canary:  v1.0.0-canary.1, v1.0.1-canary.1, …
#
# Artifacts (Electrobun names preserved — do not rename for auto-update):
#   {channel}-win-x64-Sendrova-Setup-{channel}.zip
#   {channel}-win-x64-Sendrova-{channel}.tar.zst   (canary) / stable equivalent
#   {channel}-win-x64-update.json
#   Sendrova-SMS-<version>.apk   (signed release; version from apps/android versionName)
#
# Notes:
#   - Windows builds are unsigned → SmartScreen warnings expected (see README).
#   - Android release signing requires repo secrets:
#       ANDROID_KEYSTORE_BASE64, ANDROID_KEYSTORE_PASSWORD,
#       ANDROID_KEY_ALIAS, ANDROID_KEY_PASSWORD

name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      channel:
        description: Electrobun channel
        required: true
        type: choice
        default: canary
        options:
          - canary
          - stable
      tag:
        description: "Release tag (e.g. v1.0.0). Required for branch runs; ignored when the ref is already a tag."
        required: false
        type: string

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  BUN_VERSION: "1.4"

jobs:
  resolve:
    name: Resolve channel / tag
    runs-on: ubuntu-latest
    outputs:
      channel: ${{ steps.meta.outputs.channel }}
      tag: ${{ steps.meta.outputs.tag }}
      prerelease: ${{ steps.meta.outputs.prerelease }}
    steps:
      - id: meta
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${{ github.ref_type }}" == "tag" ]]; then
            TAG="${{ github.ref_name }}"
          else
            TAG="${{ github.event.inputs.tag }}"
            if [[ -z "${TAG}" ]]; then
              echo "::error::workflow_dispatch on a branch requires inputs.tag (e.g. v1.0.0)"
              exit 1
            fi
          fi

          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            CHANNEL="${{ github.event.inputs.channel }}"
          elif [[ "${TAG}" == *canary* ]]; then
            CHANNEL="canary"
          else
            CHANNEL="stable"
          fi

          if [[ "${CHANNEL}" == "canary" || "${TAG}" == *"-"* ]]; then
            PRERELEASE=true
          else
            PRERELEASE=false
          fi

          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"
          echo "channel=${CHANNEL}" >> "$GITHUB_OUTPUT"
          echo "prerelease=${PRERELEASE}" >> "$GITHUB_OUTPUT"
          echo "Release tag=${TAG} channel=${CHANNEL} prerelease=${PRERELEASE}"

  desktop-windows:
    name: Windows desktop (${{ needs.resolve.outputs.channel }})
    needs: resolve
    runs-on: windows-latest
    timeout-minutes: 90
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: ${{ env.BUN_VERSION }}

      - name: Cache Bun install
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build Electrobun (${{ needs.resolve.outputs.channel }})
        shell: bash
        run: |
          set -euo pipefail
          CHANNEL="${{ needs.resolve.outputs.channel }}"
          if [[ "${CHANNEL}" == "stable" ]]; then
            bun run build:stable
          else
            bun run build:canary
          fi

      - name: Stage desktop artifacts
        shell: bash
        run: |
          set -euo pipefail
          CHANNEL="${{ needs.resolve.outputs.channel }}"
          mkdir -p release-assets
          shopt -s nullglob
          files=(apps/desktop/artifacts/"${CHANNEL}"-win-x64-*)
          if [[ ${#files[@]} -eq 0 ]]; then
            echo "::error::No Electrobun artifacts under apps/desktop/artifacts/${CHANNEL}-win-x64-*"
            ls -la apps/desktop/artifacts || true
            ls -la apps/desktop/build || true
            exit 1
          fi
          cp -v "${files[@]}" release-assets/
          ls -la release-assets/

      - name: Upload desktop workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: desktop-windows
          path: release-assets/*
          if-no-files-found: error
          retention-days: 14

  android-apk:
    name: Android release APK
    needs: resolve
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Ensure Gradle wrapper JAR
        working-directory: apps/android
        shell: bash
        run: |
          set -euo pipefail
          JAR="gradle/wrapper/gradle-wrapper.jar"
          if [[ ! -f "${JAR}" ]]; then
            echo "gradle-wrapper.jar missing — downloading Gradle 8.7 wrapper"
            curl -fsSL -o "${JAR}" \
              "https://raw.githubusercontent.com/gradle/gradle/v8.7.0/gradle/wrapper/gradle-wrapper.jar"
          fi
          test -s "${JAR}"

      - name: Decode release keystore
        shell: bash
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          set -euo pipefail
          if [[ -z "${ANDROID_KEYSTORE_BASE64}" ]]; then
            echo "::error::Missing secret ANDROID_KEYSTORE_BASE64. Add Android signing secrets before cutting a release."
            exit 1
          fi
          mkdir -p "$HOME/android-signing"
          echo "${ANDROID_KEYSTORE_BASE64}" | base64 -d > "$HOME/android-signing/sendrova-release.jks"
          test -s "$HOME/android-signing/sendrova-release.jks"
          echo "ANDROID_KEYSTORE_PATH=$HOME/android-signing/sendrova-release.jks" >> "$GITHUB_ENV"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: false

      - name: Assemble signed release APK
        working-directory: apps/android
        env:
          ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: |
          set -euo pipefail
          if [[ -z "${ANDROID_KEYSTORE_PASSWORD}" || -z "${ANDROID_KEY_ALIAS}" || -z "${ANDROID_KEY_PASSWORD}" ]]; then
            echo "::error::Missing one or more Android signing secrets (PASSWORD / ALIAS / KEY_PASSWORD)."
            exit 1
          fi
          ./gradlew assembleRelease --no-daemon --stacktrace

      - name: Stage APK
        shell: bash
        run: |
          set -euo pipefail
          SRC="apps/android/app/build/outputs/apk/release/app-release.apk"
          test -f "${SRC}"
          VERSION="$(sed -n 's/.*versionName = "\([^"]*\)".*/\1/p' apps/android/app/build.gradle.kts | head -n1)"
          if [[ -z "${VERSION}" ]]; then
            echo "::error::Could not read versionName from apps/android/app/build.gradle.kts"
            exit 1
          fi
          mkdir -p release-assets
          DEST="release-assets/Sendrova-SMS-${VERSION}.apk"
          cp -v "${SRC}" "${DEST}"
          ls -la release-assets/
          echo "ANDROID_APK_NAME=Sendrova-SMS-${VERSION}.apk" >> "$GITHUB_ENV"

      - name: Upload Android workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: release-assets/Sendrova-SMS-*.apk
          if-no-files-found: error
          retention-days: 14

  publish:
    name: GitHub Release
    needs: [resolve, desktop-windows, android-apk]
    runs-on: ubuntu-latest
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: List release assets
        run: ls -lah release-assets/

      - name: Create / update GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ needs.resolve.outputs.tag }}
          name: Sendrova ${{ needs.resolve.outputs.tag }}
          prerelease: ${{ needs.resolve.outputs.prerelease == 'true' }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: |
            release-assets/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
